#include <assert.h>
#include <limits.h>
#include <math.h>
#include <stdbool.h>
#include <stddef.h>
#include <stdint.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main()
{
    int i,a[100],b[100],c1=0,c2=0;
        for(i=0;i<3;i++)
        {
            scanf("%d",&a[i]);
        }
    for(i=0;i<3;i++)
        {
            scanf("%d",&b[i]);
        }
    for(i=0;i<3;i++)
        {
    if((a[i]>=1)&&(a[i]<=100)&&(b[i]>=1)&&(b[i]<=100))
    {
        if(a[i]>b[i])
            ++c1;
        if(b[i]>a[i])
            ++c2;
        
    }
        
       }
    printf("%d",c1);
    printf(" %d",c2);
    return 0;
}
